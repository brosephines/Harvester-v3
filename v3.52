
	for i, v in pairs(game.Players:GetChildren()) do
		local gui = Instance.new("ScreenGui")
		gui.Name = "willy"
		gui.Parent = v.PlayerGui

		local peta = Instance.new("ImageLabel")
		peta.Image = "http://www.roblox.com/asset/?id=4632517055"
		peta.Name = "sub to A.S.S on yt hun :D"
		peta.Parent = gui
		
		peta.Size = UDim2.new(.25, 0, .25, 0)
		peta.Position = UDim2.new(math.random(), 0, math.random(), 0)
		
		local peta = Instance.new("ImageLabel")
		peta.Image = "http://www.roblox.com/asset/?id=11583262941"
		peta.Name = "sub to A.S.S on yt hun :D"
		peta.Parent = gui
		
		peta.Size = UDim2.new(.25, 0, .25, 0)
		peta.Position = UDim2.new(math.random(), 0, math.random(), 0)
		
		local peta = Instance.new("ImageLabel")
		peta.Image = "http://www.roblox.com/asset/?id=12446425654"
		peta.Name = "sub to A.S.S on yt hun :D"
		peta.Parent = gui
		
		peta.Size = UDim2.new(.25, 0, .25, 0)
		peta.Position = UDim2.new(math.random(), 0, math.random(), 0)
		
		local peta = Instance.new("ImageLabel")
		peta.Image = "http://www.roblox.com/asset/?id=11624440776"
		peta.Name = "sub to A.S.S on yt hun :D"
		peta.Parent = gui

		peta.Size = UDim2.new(.25, 0, .25, 0)
		peta.Position = UDim2.new(math.random(), 0, math.random(), 0)
		
		
		for i, v in pairs(game.Workspace:GetChildren()) do
			if v:IsA("BasePart") then
				local sound = Instance.new("Sound")
				sound.Volume = 5
				sound.Name = "soz broe x"
				sound.Looped = true
				sound.Playing = true
				sound.SoundId = "rbxassetid://8449305114"
				sound.Parent = v
				
				
				local sound = Instance.new("Sound")
				sound.Volume = 10
				sound.Name = "blals"
				sound.Looped = true
				sound.Playing = true
				sound.SoundId = "rbxassetid://7816195044"
				sound.Parent = v
				
				local sound = Instance.new("Sound")
				sound.Volume = 7
				sound.Name = "a bear who bears no introduction"
				sound.Looped = true
				sound.Playing = true
				sound.SoundId = "rbxassetid://2050521432"
				sound.Parent = v
				
				local sound = Instance.new("Sound")
				sound.Volume = 9
				sound.Name = "zomfunkle"
				sound.Looped = true
				sound.Playing = true
				sound.SoundId = "rbxassetid://6103553029"
				sound.Parent = v
			end
		end
	end
	
	
	
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()
	
	local part2 = Instance.new("Part")
	part2.Shape = "Ball"
	
	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()

	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	local part = Instance.new("Part")
	part.Position = Vector3.new(math.random(), math.random(), math.random())
	part.Parent = game.Workspace
	part.Name = "GetTrolledROFL"
	part.BrickColor = BrickColor.random()

	local part2 = Instance.new("Part")
	part2.Shape = "Ball"

	part2.Position = Vector3.new(math.random(), math.random(), math.random())
	part2.Parent = game.Workspace
	part2.Name = "GetTrolledROFL"
	part2.BrickColor = BrickColor.random()
	
	print("Super Umbrella Ball Total Oxymoron Xylem Eradicus Radiate Aanthropomorphism Doobius Evaluate Vast Anubis Netherlands Dodecahedron Astonauight Soap Sause")
